{
  "name": "react-spa",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "bun './**/*.html'",
    "build": "bun build --target=browser professional-persona-cards.html --outdir dist"
  },
  "dependencies": {
    "react": "19",
    "react-dom": "19"
  }
}
